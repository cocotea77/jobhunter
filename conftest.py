# This file is intentionally almost empty.
#
# Its presence at the project root tells pytest (the test runner) where the
# project begins, so test files can write "from app.config import ..." and
# the import works on every machine, including GitHub's checking computer.
