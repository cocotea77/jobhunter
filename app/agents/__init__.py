"""The agent layer (Step 5).

- tailor.py       — the tailoring agent: honesty enforced by its schema
- coach.py        — the tool-using conversational agent, bounded
- orchestrator.py — the deterministic supervisor above the coach
"""
